## Agent skills

### Issue tracker

GitHub Issues on idodoron11/playlist-sync (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
